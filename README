An easy way to build a .pyx extension into a
.pyd (or .so) without having to write a 
setup.py.

Usage:

    > python easycython myextension.pyx

This will produce "myextension.pyd"
or "myextension.so" in the same folder
as the source file.
